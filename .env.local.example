# Frontend environment variables
# Copy this to .env.local and set your values

# Mock catalog for shop/cart without backend (Netlify preview: keep true)
NEXT_PUBLIC_USE_MOCK_CATALOG=true

# Only needed when connecting to a live API (not used on Netlify mock deploy)
BACKEND_URL=http://localhost:5001
